# 🚀 5分钟快速开始

## 第一步：安装依赖

```bash
pip install requests
```

## 第二步：配置

1. 复制配置文件：
```bash
cp config.json.example config.json
```

2. 编辑 `config.json`，填入你的配置：
```json
{
  "pansou_api_url": "http://localhost:8888",
  "xinyue_api_url": "http://localhost",
  "xinyue_api_key": "你的API密钥",
  "max_results": 5,
  "timeout": 30
}
```

## 第三步：测试

```python
from pansearch import PanSearchPlugin
import json

# 加载配置
with open('config.json', 'r', encoding='utf-8') as f:
    config = json.load(f)

# 创建插件
plugin = PanSearchPlugin(config)

# 搜索并转存
result = plugin.search_and_transfer("速度与激情")
print(result)
```

## 第四步：集成到你的机器人

在你的机器人代码中添加：

```python
from pansearch import PanSearchPlugin
import json

# 初始化（在机器人启动时）
with open('astrbot-pansou-transfer/config.json', 'r', encoding='utf-8') as f:
    config = json.load(f)
pansearch_plugin = PanSearchPlugin(config)

# 在消息处理函数中
def on_message(message):
    if message.startswith("/搜索"):
        keyword = message.replace("/搜索", "").strip()
        if keyword:
            return pansearch_plugin.search_and_transfer(keyword)
    return None
```

## 完成！

现在你的机器人可以响应 `/搜索 关键词` 命令，自动搜索并转存网盘资源了！

更多详细信息请查看 `使用指南.md`

